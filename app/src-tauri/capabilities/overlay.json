{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Capability for the processing overlay window",
  "windows": ["processing_overlay"],
  "permissions": [
    "core:default"
  ]
}